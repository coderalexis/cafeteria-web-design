NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
# Dominio de los correos sintéticos de las cuentas de café (usuario@slug.<dominio>).
# Debe ser un dominio que controles; nunca se muestra a los usuarios. Opcional (default: cafecitojaral.com).
SYNTHETIC_EMAIL_DOMAIN=cafecitopos.com
# API key de Resend (resumen semanal y otros envíos desde la app; el SMTP de Auth se configura en Supabase).
# RESEND_API_KEY=re_...
# Secreto que Vercel manda como `Authorization: Bearer ...` en los crons
# (/api/resumen-semanal y /api/pruebas). Sin él las rutas se niegan a correr
# para no quedar abiertas.
# CRON_SECRET=una-cadena-larga-al-azar
# A dónde avisar cuando alguien crea su cafetería desde /registro.
# OPERATOR_EMAIL=tu-correo@ejemplo.com
# URL pública del sitio; es la base del enlace de confirmación del registro.
# NEXT_PUBLIC_SITE_URL=https://cafecitopos.com
